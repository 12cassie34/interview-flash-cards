import { defineStore } from 'pinia'

export const questionSetStore = defineStore('questionSetStore', {
    state: () => {
        return {
            questionSet: [
            ],
        }
    },
    actions: {
    },
})